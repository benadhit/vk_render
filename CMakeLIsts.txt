cmake_minimum_required(VERSION 3.15)

project(hamon)

add_subdirectory(extern/glfw EXCLUDE_FROM_ALL)

add_executable(hamon 
    src/main.cpp
    src/Application.cpp
    src/VulkanUtils.cpp
    )

target_link_libraries(hamon PRIVATE glfw)